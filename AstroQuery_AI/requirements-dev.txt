# 开发依赖（与 pyproject.toml [project.optional-dependencies] dev 同步）
# 运行依赖见 requirements.txt；测试 /ruff 检查需要本文件
-r requirements.txt
pytest>=8.0
pytest-cov>=5.0
ruff>=0.5.0
mypy>=1.10
bandit>=1.7.0
pre-commit>=3.0
vcrpy>=6.0
